# ── APT Packages ─────────────────────────────────────────────────────
# One package per line. Lines starting with # are ignored.
# These are NON-DEFAULT packages that need explicit installation.
# System packages (coreutils, bash, etc.) are omitted — Ubuntu-based distros include them.
#
# For programming language runtimes, use mise instead (see mise config).
# For CLI dev tools (ripgrep, fzf, bat, etc.), use Nix home-manager.
# Only use apt for system-level apps, GUI apps, drivers, and build dependencies.

# ── Core CLI (needed before Nix is available) ────────────────────────
curl
git
unzip
wget
stow
fontconfig

# ── Build tools (needed for compiling native npm packages) ─────────────────
build-essential
cmake
ninja-build

# ── Build deps for mise-compiled runtimes ─────────────────────────────
# Erlang/OTP
autoconf
libssl-dev
libncurses-dev
libwxgtk3.2-dev

# ── Dev tools ────────────────────────────────────────────────────────
mise
wezterm

# ── Docker ───────────────────────────────────────────────────────────
docker-ce
docker-ce-cli
containerd.io
docker-buildx-plugin
docker-compose-plugin

# ── GUI applications ────────────────────────────────────────────────
code
flameshot
gimp
slack-desktop
vivaldi-stable

# ── ZSA Keyboard Flashing (Keymapp / Wally) ──────────────────────────
libusb-1.0-0             # USB device communication required by both Keymapp and Wally
libwebkit2gtk-4.1-0      # WebKit rendering engine required by Keymapp GUI
libgtk-3-0               # GTK3 UI toolkit required by Keymapp GUI

# ── Media ────────────────────────────────────────────────────────────
ffmpeg
ubuntu-restricted-extras
