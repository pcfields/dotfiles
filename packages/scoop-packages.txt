# ── Scoop Packages ─────────────────────────────────────────────────────
# One package per line. Lines starting with # are ignored.
# See https://scoop.sh and https://github.com/ScoopInstaller for more.
# These match aliases and setup for PowerShell/Fish on Windows.

# ── Core CLI (needed for config/aliases to work) ────────
git
mise                    # runtime manager; provides Node (mise use -g node@22)
nvim
eza
bat
lazygit
wezterm
zoxide
fzf
ripgrep
delta

# ── Prompt and Shell enhancers ──────────────────────────
oh-my-posh

# ── Fonts (needed for terminal/wezterm rendering) ───────
monaspacene-nerd-font
jetbrainsmono-nerd-font

# ── Dev Tools (LSP servers, formatters, linters) ────────
biome
lua-language-server
marksman
shellcheck
shfmt
stylua
# tree-sitter CLI: required by nvim-treesitter (main branch) to build parsers.
# Also needs a C compiler on PATH -- install MSVC Build Tools or `scoop install gcc`.
tree-sitter
# Node-based tooling (LSP servers, prettier) has no scoop manifest and is
# installed with npm instead -- see packages/npm-global-packages.txt

# ── Optional CLI / TUI / Docker (uncomment as needed) ───
lazydocker

# Extra buckets for scoop are required for certain tools.
# The setup script will ensure extras, versions, nerd-fonts, etc. are present.
